version: '2.1'

services:
  ${{ values.component }}:
    build:
      context: .
      args:
        - http_proxy
        - https_proxy
        - no_proxy
    image: ${{ values.acr_registry }}.azurecr.io/${{ values.product }}/${{ values.component }}
    ports:
      - $SERVER_PORT:$SERVER_PORT
